import PolarisItemBase from "./base-item.mjs";

export default class PolarisFeature extends PolarisItemBase {
  // Features only need the base item schema (description)
  // No additional fields needed
}

